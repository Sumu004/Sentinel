# Phase 2.0 — everything here is free to install and run (DECISIONS.md D8).
# Heavier ML deps (RF-DETR/YOLO12, RTMPose, ByteTrack, Qwen2.5-VL) land in
# requirements-training.txt once Phase 2.1 starts; keeping this file light
# means the core pipeline installs in seconds.

# Core perception (motion-detector default, zero model downloads)
opencv-python>=4.9
numpy>=1.26

# ByteTrack (DECISIONS.md D3) — supervision (Roboflow, MIT) ships a real,
# pip-installable ByteTrack; only imported when SENTINEL_TRACKER_BACKEND=bytetrack
supervision>=0.28

# OSNet re-ID (DECISIONS.md D3) — torchreid installs cleanly but its own
# import chain calls `import gdown` (dataset-download helpers) without
# declaring it as a dependency; gdown must be installed alongside it or
# `import torchreid` crashes with ModuleNotFoundError. Only used by
# perception/reid.py's OSNetReID.
torchreid>=0.2.5
gdown>=5.0

# Config
python-dotenv>=1.0

# Evidence chain
cryptography>=42.0
requests>=2.31
opentimestamps-client>=0.7  # real, free Bitcoin-anchored timestamping — see evidence/anchor.py

# Cloud backend (local FastAPI + SQLite by default)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Optional: only imported when SENTINEL_STORAGE_BACKEND=dynamodb
boto3>=1.34

# Federated learning (Phase 2.6) — see learning/federated_sim.py
flwr>=1.32
ray[default]>=2.5

# Tests
pytest>=8.0
